@model IEnumerable<SymphonyLimited.Models.Student>

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<main style="margin-top: 58px">
    <div class="container pt-4">
        <section>
            <div class="container-fluid mt-4">
                <table id="coursesTable" class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>SNo #</th>
                            <th>Roll No</th>
                            <th>Student Name</th>
                            <th>Email</th>
                            <th>Phone No</th>
                            <th>Course Name</th>
                            <th>Payment Mode</th>
                            <th>Payment Details</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int i = 0;
                        }
                        @if (Model != null && Model.Any())
                        {
                            foreach (var course in Model)
                            {
                                i++;
                                <tr>
                                    <td>@i</td>
                                    <td>@course.RollNumber</td>
                                    <td>@course.Name</td>
                                    <td>@course.Email</td>
                                    <td>@course.Phone</td>
                                    <td>@course.Course.CourseName</td>
                                    <td>@course.PaymentMode</td>
                                    <td>@course.PaymentDetails</td>
                                    <td>
                                        <a asp-action="Restore" asp-route-id="@course.StudentId" class="btn btn-sm btn-danger">Restore</a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="9" class="text-center">No Student available</td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </section>
        <!--Section: Statistics with subtitles-->
    </div>
</main>